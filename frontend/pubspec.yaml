name: kuza_shield
description: Cyber security app for Kenya with Airtel integration.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  hive: ^2.0.0
  hive_flutter: ^1.0.0
  dio: ^4.0.0
  http: ^0.13.0
  file_picker: ^5.0.0
  url_launcher: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true